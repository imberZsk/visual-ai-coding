# 完成标准

- 需求已实现，空值、失败、回滚、重复提交、任务持续性和跨平台分支已检查。
- 新增或修改行为有对应测试，未补无关历史覆盖率。
- 至少运行相关 Vitest、`pnpm run lint` 与 `pnpm run build`。
- 主进程、preload 或 IPC 改动再运行 `pnpm run verify:boot`。
- 本任务是 AI 规则整理，按用户要求不修改功能清单、CHANGELOG 或版本号。
- 最终说明实际验证结果、未验证项和残余风险，不声称未执行的检查通过。
