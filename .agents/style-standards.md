# 样式规范

- Tailwind 使用语义色类名和 CSS 变量，如 `bg-surface`、`text-text-muted`；禁止写死只适配单一主题的颜色。
- 延续现有 Ant Design、间距、字号和状态反馈，同时检查浅色、深色与跟随系统主题。
- 优先使用主题 token、现有 utility class 和组件属性，避免重复内联样式。
- 长配置路径、插件名和错误文本支持省略、换行或 Tooltip，不得挤出主要操作。
- loading 和动态内容使用稳定尺寸，避免切页或刷新时布局跳动。
- 不引入营销式装饰、无业务意义动画或与工具型界面不一致的大圆角卡片。
